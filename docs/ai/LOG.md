# LOG — สมุดรายวัน (append-only: เติมท้ายไฟล์เท่านั้น ห้ามลบของเก่า)
รูปแบบ: `## วันที่ | ใครทำ` แล้วตามด้วย ทำอะไร / ผล / ค้างอะไร

## 15 ก.ค. 2026 | Claude (เครื่องออฟฟิศ)
- เคสคุณชิต (ลูกค้าจ่าย 49 คะแนนซ้ำทุกองค์): พบ cross-object seed collision → แก้ extractor v2 (identity slugs 6 ช่อง), ลบสแกนซ้ำ+baseline, เติมสิทธิ์ 15 ครั้ง, ฝาก admin_case_note ให้บอทเล่าตรงกับแอดมิน
- เซ็นเซอร์แท่นรางวัลคลัง: อันดับ 1-2 ดูได้เฉพาะสมาชิกรายเดือน
- ค้าง: รอบสองเจอซ้ำติดกัน → ทำ dHash เข้า seed (จบ 16 ก.ค.)

## 16 ก.ค. 2026 | Claude (เครื่องออฟฟิศ)
- dHash ผสม score seed เลนพระ (กันคนละองค์ slug เดียวกันคะแนนชนกัน) — LIVE pro
- ค่า OpenRouter แพง: เปิด prompt cache (ประหยัด 91% ต่อข้อความ) + แยกสองชั้น จ่าย=Opus/ฟรี=DeepSeek V4 Flash ตอบสั้น — LIVE pro
- การ์ดลงทะเบียน Flex (พื้นขาว+โลโก้+ปุ่มทอง) — LIVE pro
- การ์ดผลแชร์ได้ /r/:token/card.png ธีมทองเข้ม + ปุ่มแชร์ 3 จุด + og:image — LIVE pro
- แอด: ปิด VDO, งบ 200→500, End date=Ongoing (ระวัง cap ระดับแคมเปญ ฿200 — นัดเช็ค 20 ก.ค.)

## 17 ก.ค. 2026 | Claude (เครื่องออฟฟิศ)
- การ์ดแชร์โฉมหรู (ดำทอง วงมนตร์ เรดาร์ 6 แกน) — LIVE pro+staging
- แก้บั๊กสระ ำ เพี้ยนบนการ์ด (resvg วาด U+0E33 สระ า หาย → แตกเป็น ํ+า ใน escapeXml)
- เพิ่มแถว "เข้ากับดวงเจ้าของ ★+%" บนการ์ด (จาก summary.compatibilityPercent ซ่อนถ้าไม่มีวันเกิด) — LIVE pro
- ตรวจ log แชทหลังสลับ 2 ชั้น: DeepSeek ชั้นฟรีตอบดี ไม่มีหลุดบท / flow จ่ายเงินปกติ (จ่าย 49 อนุมัติ 16 ก.ค. เช้า)
- ตั้งห้องทำงาน kob-dev บน Contabo VPS (161.97.165.70): vscode.dev/tunnel/kob-dev + RDP ผ่าน Tailscale (100.80.106.63) + Claude Code + repo ทั้งคู่ + ssh ener (กุญแจแยกใบ)
- สร้างโครงบันทึกงาน AI (AI-README/STATE/BACKLOG/LOG) ตามสั่งกบ
- ค้าง: แชร์แลกสิทธิ์ +1 รอเคาะ · เฝ้า DeepSeek ต่อ · นัดแอด 20 ก.ค.

## 17 ก.ค. 2026 | Claude (kob-dev VPS)
- ตรวจสุขภาพ pro ผ่าน ssh (docker logs) — ระบบแชท/สแกนปกติดี ไม่มี error จริงสักตัว
- 24 ชม.: webhook 46 event / สแกนสำเร็จส่งผล 34 / ล้ม 2 = ทั้งคู่ forensic_flagged (ท้าถ่ายสด เคสคนลองของ ทำงานถูกตามดีไซน์) · คอนเทนเนอร์ pro ขึ้นครบ
- เลข "error 42,880" ใน worker-delivery เป็น false positive (log OUTBOUND_CLAIM_RPC_RAW มี field hasError:false — worker วน poll คิวส่ง ไม่ใช่ error)
- flow ครบสเต็ป: รับรูป→object-check gpt-4.1→queue→สแกนจบ→ส่ง summary_link+Flex+การ์ด · paywall/quota เด้งถูก
- ข้อสังเกต: แชท consult ชั้น 2 (Opus/DeepSeek) ยังไม่มีใครเรียกเลยใน 72 ชม. — ลูกค้ายังไม่ "ถามต่อ" กับอาจารย์ → งานเฝ้า DeepSeek ชั้นฟรี (นัด ~19 ก.ค.) ยังไม่มี data จริงให้ดู
- ค้าง: อยากได้ data ชั้นฟรี ต้องยิงเทสต์แชทถามต่อในบัญชีเทสต์ก่อนถึงวันนัด

## 17 ก.ค. 2026 (บ่าย) | Claude (kob-dev VPS)
- **โปรใหม่ jul2026 v2** (กบเคาะสุดท้ายหลังปรึกษา 4-AI + วนหลายรอบ): ฟรี 1/วัน (คงเดิม) · **29 "รายองค์" = 1 ครั้ง/24 ชม** (ประตูจ่ายครั้งแรก) · ⭐**49 "เปิดพลัง" = 4 ครั้ง/24 ชม** (default, ตัวเดิมที่พิสูจน์แล้ว ก๊อปแอดไม่ต้องแก้) · **399 "สมาชิกอาจารย์" = 30 ครั้ง/30 วัน** — **เลิกขาย unlimited ถาวร** (whale เคสจริง: 25 สแกน/6 ชม บนแพ็ก 299 เดือน) · แพ็ก 149/299 เดิมถูกถอด
- อัปเดต scanOffer.default.json เป็นโปรใหม่ (SAFE_DEFAULT ในโค้ดคงเดิมเป็นหลุมหลบภัย) — **ของจริงขึ้นผ่าน /admin/promo (DB override) วันที่ 21 ก.ค.** หลังนัดดูผลแอด 20 ก.ค. เพื่อไม่ให้ data ปน
- **เกตใหม่ "เคยจ่ายเงินสักครั้ง" (hasEverPaid)** แทนเกตรายเดือน/แพ็กแอคทีฟเดิม: เคยจ่าย = เห็นคลังเต็ม+อันดับ 1-2+หนุนดวงวันนี้+เสียงอาจารย์ตลอด / **ไม่เคยจ่าย = ล็อกคลังทั้งหน้า** (หน้า /library + คลังย่อในรายงาน เหลือการ์ดล็อก+CTA เปิดสิทธิ์) และไม่ได้ข้อความเสียง (ตัดเงื่อนไขสแกนแรกฟรีออก — ประหยัด ElevenLabs)
- helper ใหม่ src/services/everPaid.service.js (payments status=paid → fallback paid_until เคยมีค่า, มี cache) — จุดใช้: report.controller ×2, liff.routes ×2 (daily-pick API + teaser), scanVoiceNote
- ก๊อป "สมาชิก 299 / สมาชิกรายเดือน" ในเทมเพลต 4 ไฟล์ → "เปิดสิทธิ์เพื่อดู" (เกตใหม่ไม่ผูกราคา) · ตัวอย่างราคาใน geminiPhrasingPrompt คง 49
- ลูกค้าเก่า 2 คนแพ็กเดือน grandfather (สิทธิ์ใน app_users อยู่จนหมดอายุ + เกตใหม่ยิ่งเห็นครบ) · renewalReminder ตรวจแล้วชวนเข้าแพ็กใหม่อัตโนมัติตอนหมดรอบ
- เทสต์: อัปเดต 5 ไฟล์เทสต์ให้ตรงโปรใหม่ — ชุดเต็ม 923 pass / 17 fail เท่า baseline HEAD เป๊ะ (17 ตัวพังอยู่แล้วบนเครื่องนี้ ต้องพึ่ง redis/DB จริง) · test:release 38/38
- ค้าง: deploy staging + เทสต์จริง · ขึ้น pro + ใส่ override ผ่าน /admin/promo = รอกบสั่ง 21 ก.ค. · เฟส 2: เตือน whale อัตโนมัติ, สิทธิพิเศษสมาชิกอาจารย์ (คลังพระ/Daily Match ผูก 399)

## 17 ก.ค. 2026 (เย็น) | Claude (kob-dev VPS)
- **โปรใหม่ v2 + เกตเคยจ่าย ขึ้น pro แล้ว** (กบสั่งขึ้นเลย ไม่รอ 21 ก.ค.): cherry-pick b2ad884 เข้า main → deploy pro blue-green healthy → สลับ override /admin/promo เป็น promo_jul2026_v2
- ตรวจหลังขึ้น: paywall pro โชว์ 29/49/399 ถูกต้อง · ฟรี 1/วัน · 2 คนแพ็กเดือนเดิมสิทธิ์คงเดิม (U277f747 หมด 12 ส.ค. / Ub275536 หมด 16 ส.ค.) · https 200 · ไม่มี error หลังบูต
- ผลที่ตามมา: ลูกค้าใหม่ไม่มีทางซื้อ unlimited อีก · คนไม่เคยจ่ายเห็นคลังล็อกทั้งหน้า+ไม่มีเสียง · จ่ายครั้งเดียว (แม้ 29) ปลดคลัง/อันดับ/หนุนดวง/เสียงถาวร
- ค้าง: เฝ้า conversion ฟรี→29→49 + ยอด/บิล 2 สัปดาห์ · นัดแอด 20 ก.ค. (จำไว้ว่าโปรเปลี่ยน 17 ก.ค.เย็น — อ่าน funnel ให้แยกช่วงก่อน/หลัง) · ฟีเจอร์สมาชิก 399 (Daily Match/บัตร) ตาม backlog

## 17 ก.ค. 2026 (ค่ำ) | Claude (kob-dev VPS)
- reset บัญชีเทสต์กบ (Ufe02fff) เป็น "ไม่เคยจ่าย" บน pro ตามสั่ง: บิล paid 157 ใบ → status void_test_reset_20260717 (กู้คืนได้) + paid_until null (เดิม 2027-07-09, เหลือ 72 ครั้ง — จดไว้คืนภายหลัง) + redeploy ล้างแคช
- **paywall โควตาฟรีหมด → การ์ด Flex ดำทอง** (กบ: "เอา flex + reply"): การ์ดโชว์ครบ 3 แพ็กจาก config + ⭐ ไฮไลต์แพ็กเด่น + ปุ่มจ่ายในการ์ด (หลักทอง "จ่าย 49 เปิดเลย" + รอง 29/399) + quickReply เดิม — ราคาแก้ที่ /admin/promo การ์ดตามเอง
- ก๊อปข้อความ (เป็น altText/fallback ด้วย) เปลี่ยนเป็นโชว์ครบทุกแพ็กแบบสั้น: "องค์เดียว 29 / 4 ครั้ง 49 ตกองค์ละ 12 คุ้มสุด / 30 ครั้ง 399 ตลอด 30 วัน" — "ตกองค์ละ" โชว์เฉพาะแพ็กเด่น (ไม่กางเลขให้ 399 เสียเปรียบ)
- ของใหม่: src/services/flex/paywallOffer.flex.js · gateway รองรับ flexMessage (การ์ดแทน text, sticker งดในโหมดการ์ด) · test:release 38/38
- ปรับการ์ด paywall ตามกบ: พื้นขาวคาดทอง (ไม่เด่นแข่งการ์ดรายงาน) + แยกชื่อแพ็ก/รายละเอียดคนละบรรทัด แก้ตัดคำ ("24 ชม." ตกบรรทัด) · แถวเด่นครีมกรอบทอง ราคาใหญ่ขวา
- เคสกบเจอจริง: พิมพ์ "จ่าย" แล้วการ์ดไม่ขึ้น (เส้นเมนูเลือกแพ็กเป็นคนละ path) → เสียบการ์ดใบเดียวกันเข้า payment_pick_package_menu หัวการ์ดปรับบริบท ("เปิดสิทธิ์เพิ่มวันนี้") — ตอนนี้การ์ดขึ้นทั้งสองเส้น: โควตาหมด+ส่งรูป / พิมพ์ จ่าย
- จูนการ์ดตามกบเทสต์จริง 3 รอบ: ขยายช่องราคา (399.- เคยโดนตัดเป็น 39...) · บรรทัดรอง wrap · เพิ่มปุ่ม "ไว้ก่อน" (link จาง) ท้ายการ์ด + quickReply เมนูจ่าย — LIVE pro+staging
- **บั๊กกบเจอ (สำคัญ): ส่งรูปพระตอนมีรายการรอสลิป → ระบบยัดเข้าคิวตรวจสลิป + เด้งแอดมิน** ทั้งที่ vision ปัดถูกว่า object_photo (gate reject ก่อนเช็ควัตถุ → objectCheckResult ว่าง → หลุด soft-pass) — แก้: เคส vision ฟันธง object_photo ให้เช็ควัตถุยืนยันซ้ำ ถ้าเป็นวัตถุมงคลจริง → ลอง reroute เข้าสแกน (มีสิทธิ์+วันเกิด) ไม่ได้ก็ตอบตรง ๆ "รูปนี้เป็นวัตถุมงคล ไม่ใช่สลิป" คงสถานะรอสลิปเดิม ไม่เข้า pending_verify · สลิปกำกวมยัง soft-pass ตามกติกาเดิม (กันเคสสลิปกรุงเทพ)
- **บั๊กกบเทสต์ (2 จุด)**: (1) paywall เส้นที่ 3 หลังสแกนเสร็จ (deliverOutbound worker-delivery) ยังเป็นข้อความ → ติดการ์ด Flex ใบเดียวกัน หัว "สิทธิ์สแกนวันนี้ครบแล้ว" (pushFlex แทน pushText เฉพาะเคสโควตาหมด) (2) เสียงหลุดให้คนไม่เคยจ่าย เพราะ app_settings.voice_note.audience="all" bypass เกต hasEverPaid → เปลี่ยนเป็น "paid" บน pro+staging (โค้ด hasEverPaid ถูกอยู่แล้ว แค่ config override ค้าง)
- ตอนนี้การ์ดโปรขึ้นครบ 3 เส้น: โควตาหมด+ส่งรูป / พิมพ์ จ่าย / หลังสแกนเสร็จ
- **บั๊กกบชี้ (flow จ่ายเงิน): "ไว้ก่อน" ตอนรอโอน (awaiting_slip) ไม่ยกเลิกรายการ** — Gemini แค่ตอบรับ "ครับ ตามนั้น" แต่ awaiting_payment ยังค้าง → รูปถัดไปยังโดนมองเป็นสลิป — แก้: ดัก "ไว้ก่อน/พรุ่งนี้/ยังไม่เอา/เดี๋ยวก่อน" ตอน awaiting_slip → cancelAwaitingPaymentOnDefer (expired เฉพาะ awaiting_payment ไม่แตะ pending_verify) + clearPaymentState + ตอบ "ได้เลยครับ ไว้พร้อมเมื่อไหร่พิมพ์ จ่าย หรือส่งรูปมาสแกนใหม่" → หลุด flow จริง รูปถัดไปเข้าเลนสแกนปกติ
- จูนการ์ด paywall ตามกบ: ตัวหนังสือเล็กลง (หัว lg→md, ชื่อแพ็ก md→sm, รายละเอียด xxs, ราคา xl→lg) · เอา ⭐/✨ ออกทั้งหมด (ดู AI) · "คนเลือกมากที่สุด" ที่ถูกตัด → เปลี่ยนเป็นป้าย "แนะนำ" ใน baseline box ข้างชื่อแพ็ก (ไม่ตัดคำ) · ปุ่มหลักเหลือ "จ่าย 49"
- **กบเปลี่ยนใจเรื่องคลัง (ไม่เคยจ่าย)**: จากเดิม "ล็อกซ่อนทั้งหน้า" (lockedAll) → **เห็นภาพคลังทั้งหมดแต่เบลอ+เซ็นเซอร์ทุกชิ้น** (censorAll) มีปุ่มเปิดสิทธิ์ทุกชิ้น + แบนเนอร์บนสุด — เห็นโครง podium/แถว/แท็บครบ ภาพเบลอ ไม่โชว์ report ID/ชื่อ · เคยจ่าย = ชัดครบเหมือนเดิม · แก้ทั้งมินิในรายงาน (amuletReportV2) + หน้าคลังเต็ม (amuletLibraryRanking ใช้ flag censorAll ใน panelHtml/podiumCardHtml)
- ตามต่อ: carousel "พระเด่นประจำพลัง" (axisHighlights) ยังไม่ได้เซ็นเซอร์ (ภาพชัด+โชว์ ES-ID) → เพิ่ม censorAll ทั้ง 2 ที่ (axisHighlightCardHtml + buildLibraryAxisHighlightsMiniCarouselHtml) เบลอภาพ+ซ่อน ID+ปุ่มเปิดสิทธิ์ · เสียง: ยืนยันซ้ำ audience=paid ตัดเสียงคนไม่เคยจ่ายที่ใช้ฟรีทุกวันแล้ว (hasEverPaid gate)
- เคสลูกค้าจริง (บอมบ์ U90642fc60): กดเพศชายแต่บันทึกเป็นหญิง (toggle LIFF เขียนถูก = กดพลาด ไม่ใช่บั๊กระบบ) — แก้ DB ให้เป็นชายแล้ว + **เพิ่มแก้เพศ inline บนหน้า /admin/users** (dropdown ชาย/หญิง/ไม่ระบุ auto-save) : route POST /admin/users/:id/profile + updateLiffProfileByAdmin (แก้ gender/nickname ใน liff_profiles, validate ค่า) — กบแก้เคสแบบนี้เองได้แล้วไม่ต้องแตะ DB
- หน้าโหลด LIFF: ทำ animation แนว "เรียกพลัง" (กบเลือก) — sigil วาดตัวเอง draw-on (pathLength=1 stroke-dashoffset) เรียงวงนอก→รัศมี→เพชร→จุดกลาง pop + ออร่าทองแผ่เป็นวง (aura pulse) + ชื่อ Ener fade-up+แสงทองกวาด (shine) · reduced-motion เห็น static เต็ม · **ขึ้น staging อย่างเดียวก่อน รอกบเคาะค่อยขึ้น pro**

## 18 ก.ค. 2026 | Claude (kob-dev VPS)
- **เคสลูกค้าจริง 7Kendo**: กดจ่าย 29 → เปลี่ยนใจ 49 → ได้ QR 2 ชุด + ข้อความ "สรุปยอด 29" ค้างท้ายแชท (ข้อความใน bundle timestamp วินาทีเดียวกัน แอป LINE เรียง tie สลับได้) → โอน 29 เข้ารายการ 49 ติดคิวแอดมิน — กบสั่งอนุมัติเป็น 29: สลับรายการ PAY-8D41DF8D เป็นแพ็ก 29 + markPaymentApprovedAndUnlock + แจ้งลูกค้า (ได้ 1 ครั้ง/24 ชม) เรียบร้อย
- **แก้ต้นเหตุ 4 ชั้น (กบเคาะ)**: ① สลับแพ็กครั้งแรก → intro บอกชัด "เปลี่ยนจากแพ็ก X เป็นแพ็กนี้ให้แล้ว รายการเดิมยกเลิก ยึดรายการนี้อันเดียว" (switchedFromPriceThb ใน buildPaymentQrIntroText) ② สลับครั้งที่ 2+ ใน 10 นาที (redis pay_switch counter) → ไม่ยิง QR ยกเลิกรายการค้าง ถาม "สรุปจะเอาโปรไหนดีครับ" + ปุ่มทุกแพ็ก+ไว้ก่อน ③ ยุบ QR bundle 4→2 ข้อความ (สรุป+วิธีสลิปรวมก้อน, QR ปิดท้าย, ตัดสติกเกอร์) กันเรียงสลับ ④ หน้า admin เพิ่ม "อนุมัติเป็นแพ็กนี้" (dropdown เลือกแพ็กตามยอดโอนจริง — route /admin/payments/:id/approve-as + switchPendingPaymentPackageByAdmin) บนหน้า detail + การ์ดมือถือ
- เทสต์: release 38/38 · จุดที่แตะผ่านหมด (fail 1 = buildPaymentQrIntroText test พังอยู่แล้วบนเครื่องนี้ก่อนแก้)
- คืนสิทธิ์บัญชีเทสต์กบ (Ufe02fff) แล้ว: บิล 157 ใบ→paid, paid_until 2027-07-09, 72 ครั้ง
- **audit ซ้ำตามกบสั่ง (never-paid ทุก surface)**: พบรั่วที่**เลนกำไลหิน** — คลังกำไล (spotlight อันดับ 1 + กำไลเด่นประจำพลัง) โชว์เต็มให้ทุกคน เพราะ renderer ไม่ส่ง memberAccess ให้เลน crystal/moldavite เลย → แก้: ส่งเกตครบทุกเลน + buildCrystalBraceletLibraryHtml รองรับ censorAll (เบลอรูป+ปุ่มเปิดสิทธิ์+แบนเนอร์) · เช็คแล้ว: เลนพระ (mini+หน้าเต็ม+carousel) ✓ เสียง audience=paid ✓ moldavite ไม่มีคลัง (มีแต่ pickTeaser ที่เบลอขายอยู่แล้ว) ✓ สแกนฟรี+report ครั้งนั้นดูได้ปกติ ✓
- **consult แนะนำพระ (กบเห็นแชทลูกค้าจริงถาม "ดวงฉันเหมาะพระด้านไหน")**: prompt-only เพิ่มกติกาในบล็อก SCAN HISTORY — เฉพาะเมื่อลูกค้าถามแนวแนะนำพระเอง (ไม่ถาม=ไม่เสนอ): ① เช็คประวัติสแกนก่อน มีชิ้นตรงด้าน→อ้างชิ้นจริงของลูกค้า+ลิงก์รายงาน ② คลังว่าง/ไม่ตรง→แนะนำแนวตามหลัก แล้วชวนส่งพระสมเด็จ/เครื่องรางที่บ้านมาสแกน ③ NO GUESSING: ห้ามแต่งดวง/ชื่อพระ/รุ่น/คำทำนายที่ไม่มีใน context (ไม่มีข้อมูลดวง=ห้ามมโนวันศรี) — ไม่แตะ code path ใด ไม่กระทบ flow อื่น (ข้อมูลคลังส่งเข้า prompt อยู่แล้วผ่าน buildScanHistoryContext)
- **เคสลูกค้า 7Kendo ถามหาพระโชคลาภ/เมตตา (กบสั่งส่งมือ)**: ดึงคลังจริงจาก DB (11 ชิ้น ตัวจัดอันดับเดียวกับหน้าเว็บ) → ส่งข้อความเสียงอาจารย์: โชคลาภสุด ES-RPTVXA5T (แกน 79) + เมตตาสุด ES-RPTIZTBW (แกน 78) + ลิงก์ 2 ชิ้น — ส่งสำเร็จ ลูกค้าอ่านแล้ว
- **consult ตอบคะแนนรายแกนอัตโนมัติ**: buildAxisTopContext ใหม่ (reuse sacredAmuletLibrary axisHighlights + byOverall/byFit) ต่อเข้า consult prompt เป็นบล็อก "ชิ้นเด่นสุดแยกตามด้าน" — ลูกค้าถาม "ด้านโชคลาภชิ้นไหนแรงสุด" AI ตอบถูกชิ้น+ส่งลิงก์เอง ตรงกับหน้าคลัง
- **บั๊กกบชี้ (แท็บคลังรายด้าน)**: แท็บ "เมตตาสูงสุด" เรียงถูกตามคะแนนแกน แต่เลขใหญ่ที่โชว์คือพลังรวม+ป้ายเด่นสุดของแกนอื่น (ดูเหมือน filter เพี้ยน เช่น อันดับ 1 โชคลาภโชว์ 73 ทั้งที่แกนโชคลาภ 79) → แท็บรายด้านโชว์คะแนนแกนนั้น + คำว่า "คะแนนด้านนี้" (rankCardHtml รับ axisKey)
- **สติกเกอร์หมีติดไปกับข้อความระบบ (กบถาม bug ไหม)**: enqueueAdminSystemText ยืมช่อง approve_notify ซึ่งแนบสติกเกอร์ฉลองเสมอ → เพิ่ม payload.noSticker=true + delivery เคารพ flag — ข้อความระบบ/แอดมินไม่ติดสติกเกอร์อีก (approve จริงยังมีสติกเกอร์ตาม design)
- **consult "ทำยังไงให้พระพลังแรงขึ้น" (กบ dictate)**: prompt-only เพิ่มหลักตอบ — ① พลังอยู่ที่เจ้าของด้วย: ทำบุญ สวดมนต์ นั่งสมาธิจิตนิ่ง ทำความดีสม่ำเสมอ + เรื่องกรรมมีผล (พูดเชิงแนะนำ ไม่ขู่) ② จังหวะเสริมพลังเฉพาะชิ้น (วันส่งดี/ช่วงเวลา/แนวใช้) มีในรายงานอยู่แล้ว → แนบลิงก์ชิ้นจากประวัติสแกน ให้เปิดดูส่วนจังหวะเสริมพลัง — ห้ามเดาวัน/เวลาเอง
- เพิ่มหลักสายพลังงาน (กบ dictate): แต่ละคนรับพลังงานไม่เท่ากัน ขึ้นกับสภาพจิตช่วงนั้น สิ่งที่เคยทำมา การฝึกจิต — สมาธิสม่ำเสมอ→จิตสงบ→ดึงดูดสิ่งดี ๆ เข้ามาเอง · ใช้อธิบายความต่างรายบุคคลด้วย (ทำไมชิ้นเดียวกันแต่ละคนรับพลัง/เข้ากันต่างกัน)
- **เกตเซ็นเซอร์ใหม่ 3 วัน (กบสั่งเพิ่ม)**: จาก "ไม่เคยจ่าย=เซ็นเซอร์" → **"จ่ายใบล่าสุดเกิน 3 วัน=เซ็นเซอร์"** (hasRecentPaidAccess: paid_until ยังไม่หมด หรือ payments.verified_at ภายใน 72 ชม → เปิด) — 399 เปิดตลอด 30 วันผ่าน paid_until, 2 คน grandfather ไม่โดน (paid_until ยังอยู่), แอดมินเติมสิทธิ์ครอบด้วย · สลับ caller 4 จุด (report.controller x2 + liff daily-pick x2) · **เสียงอาจารย์คงเกตเดิม hasEverPaid ไม่แตะ** (กบไม่ได้สั่ง) · จุดประสงค์: ดึงคนกลับมาจ่ายซ้ำ
- **จำแนกประเภทวัตถุ เฟส 1+2 (เคสธูปหวย — กบเคาะหลังสังเคราะห์คำตอบ 3 AI)**: ขยาย prompt ตัว extractor v2 (gpt-4.1-mini call เดิม ต้นทุนเพิ่ม ~0) เพิ่ม objectForm 17 slug + formConfidence + motifFamily 14 slug + motifConfidence + sensitiveFlags — **10 slug เดิม/seed/คะแนน ไม่แตะเลย คง version v2** (cache คะแนนเดิมไม่หลุด) · โมดูลใหม่ src/services/objectTaxonomy/objectTaxonomy.js: whitelist slug (นอกลิสต์→unknown, model ห้ามตั้งชื่อ) + ชื่อไทย + เกณฑ์ความมั่นใจ (form ≥.85 พูดตรง / .65-.84 "ลักษณะใกล้เคียง" / motif ≥.90 / .72-.89 "คล้ายแนว" / ต่ำกว่า→ไม่โชว์ ไม่โชว์เลข %) + deriveUsageProfile (ธูป/เทียน=ของใช้พิธี, รูปตั้ง/ผ้ายันต์/มีดหมอ=ตั้งบูชา → ห้าม "พกติดตัว")
- เธรดผ่าน: processScanJob → buildReportPayloadFromScan (ทั้ง 2 เลน) → payload.object.objectUnderstanding + normalize pass-through + เก็บใน object_baseline_json (reuse ข้ามบัญชีโชว์ได้เหมือนสแกนสด) · กรอง tips "พกติดตัว/ห้อยคอ/สวมใส่" อัตโนมัติเมื่อพกไม่ได้ (ถ้ากรองแล้วว่าง→คืนของเดิม ไม่ทำรายงานโหว่) · โชว์ hero รายงาน (amuletV2 + mobile): "ลักษณะที่อ่านได้: ธูปบูชา · ลวดลายแนวท้าวเวสสุวรรณ/ยักษ์ (อ่านจากภาพเท่านั้น ไม่ยืนยันรุ่น/ที่มา)" + บรรทัดข้อควรรู้การใช้ · consult context ได้ "ลักษณะที่อ่านได้ + ข้อควรรู้การใช้" ต่อชิ้น (AI เลิกแนะนำให้พกธูป)
- กติกากันพลาด: prompt สั่ง form=สิ่งที่วัตถุ "เป็น" ไม่ใช่ลายที่พิมพ์ (ธูปลายท้าวเวสฯ≠องค์ท้าวเวสฯ) + ข้อความในภาพเป็นแค่ภาพ ห้ามเป็นคำสั่ง (กัน prompt injection) + sensitiveFlags (เขี้ยว/กระดูก) ใช้ภายใน ไม่ฟันธงชนิดสัตว์ · เฟส 3 (ชื่อเฉพาะ เช่น หลวงปู่ทวด/ไอ้ไข่ + ปุ่ม feedback ตรง/ไม่ตรง) → BACKLOG
- เทสต์: objectTaxonomy 5/5 ใหม่ · release 38/38 · full suite 923 pass / 17 fail (ชุด baseline เดิมของเครื่อง ต้อง redis/DB จริง — เทียบแล้วตรงชุดเดิม)
- **เกตธูป/เทียนไม่ให้คะแนน (กบเห็นธูปหวยได้ 7.4/10 เข้ากับคุณ 77% บน staging — "มุขนี้ดีไหม" → เคาะ)**: evaluateRitualScanGate ใน objectTaxonomy — AI มั่นใจ ≥85% ว่าเป็นธูป/เทียน → ไม่อ่านพลัง+**คืนสิทธิ์** (deleteScanResultForAppUser แบบเดียว lane-reject) ตอบสั้น **ไม่ฟันธงว่าเป็นอะไร** (กบสั่ง): "ชิ้นนี้อาจารย์อ่านพลังให้ไม่ได้ครับ ไม่ตัดสิทธิ์ — เปลี่ยนชิ้นอื่นส่งมาได้เลย" · ก้ำกึ่ง (50-84%) → ขอรูปมุมตรง ๆ เต็มชิ้น 1 รอบ (redis ritual_angle_asked TTL 2 ชม — ครั้งถัดไปปล่อยอ่านปกติ กันถามวน) + คืนสิทธิ์ · พระ/เครื่องราง/อ่านไม่ออกทั่วไป = ผ่านปกติ (เกณฑ์ reject สูงกันพระรูปมืดโดนลูกหลง) · baseline reuse เช็คจาก objectUnderstanding ใน baseline JSON ด้วย · จุดเสียบ: processScanJob หลัง extractor ก่อน build payload (fail-open ถ้า gate พังให้สแกนต่อ) · ข้อจำกัดที่บอกกบแล้ว: ธูปแผ่นแบนทรงเหมือนพระผง AI อาจอ่านเป็นพระพิมพ์มั่นใจสูง → หลุดได้ ต้องเก็บเคสจริงจูนต่อ · เทสต์ gate 6/6 + invariants + release 38/38
