# Standalone sidecar shared by pro + staging stacks (stateless — one instance).
# Run: cd vision-sidecar && docker compose up -d --build
services:
  vision:
    build: .
    container_name: ener-vision
    restart: unless-stopped
    ports:
      - "127.0.0.1:8077:8077"
    mem_limit: 2600m
    cpus: 2.0
